flask==3.0.0
Werkzeug==3.0.0
flask-migrate==4.0.5
flask-sqlalchemy==3.1.1
Flask-Dance==7.0.0
Flask-Minify==0.42
flask_wtf==1.1.2

sqlalchemy==2.0.52

blinker==1.6.2
pyOpenSSL==23.2.0

gunicorn==21.2.0
python-dotenv==1.0.0

# Import d'articles depuis un fichier Excel (voir apps/import_articles.py)
# + export Excel (voir apps/exports.py)
openpyxl==3.1.5

# Export PDF (voir apps/exports.py) — pure Python, pas de dépendance système.
reportlab==5.0.1

# Rapprochement Sage 100 (voir apps/sage_connector.py) — nécessite aussi le
# driver ODBC système correspondant à l'édition Sage 100 utilisée
# (ODBC Driver for SQL Server pour Sage 100cloud, driver Actian/Pervasive
# PSQL pour Sage 100 Standard), installé séparément sur le serveur.
pyodbc==5.3.0

# flask_mysqldb==1.0.1
# psycopg-binary==3.1.12
